import { ModuleComingSoon } from "@/components/shared/module-coming-soon";

export const metadata = { title: "Vendors" };

export default function VendorsPage() {
  return (
    <ModuleComingSoon
      title="Vendors"
      phase="Phase 5"
      description="Vendor directory, contacts and performance tracking."
    />
  );
}
